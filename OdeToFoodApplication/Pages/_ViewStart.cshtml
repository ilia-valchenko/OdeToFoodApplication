

<!-- 
    It's a bit of a magical file :)
    When ASP .NET Core goes to render a view or a page it also looks for
    _ViewStart file. If it finds the file it always execute the code inside a code block.
    The code will apply to every page inside Pages folder.
-->

@{
    Layout = "_Layout";
}